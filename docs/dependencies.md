# 依赖审计记录

本文档记录所有外部依赖的选择理由和审计结果，遵循项目宪章**原则 3：依赖审计**。

---

## 核心依赖（生产环境）

**当前状态**: ✅ 零依赖

根据**原则 2：零依赖目标**，核心功能完全基于 Python 标准库实现。

### 使用的标准库模块

| 模块 | 用途 | 文件 |
|------|------|------|
| `time` | 计时装饰器 | `aoplib/stage.py` |
| `functools` | `@wraps` 保留元数据 | `aoplib/stage.py` |
| `typing` | 类型注解 | 待添加 |

---

## 可选依赖（extras）

**当前状态**: 无可选功能依赖

如未来添加扩展功能（如异步支持、类型检查增强），将记录在此。

### 模板：如何记录可选依赖

| 包名 | 版本 | extras 组 | 用途 | 审计日期 | 状态 |
|------|------|-----------|------|----------|------|
| `aiohttp` | >=3.8.0 | `async` | 异步 HTTP 客户端 | 2025-10-28 | ✅ 通过 |

**审计详情**:
- **维护活跃度**: ✅ 最近 6 个月有更新（[GitHub链接]）
- **社区规模**: ✅ Stars: 14k+, PyPI 下载: 50M+/月
- **许可证**: ✅ Apache 2.0（兼容）
- **传递依赖**: ✅ 3 个（`aiosignal`, `frozenlist`, `yarl`）
- **选择理由**: 标准库 `asyncio` 不提供高级 HTTP 客户端，`aiohttp` 是异步生态标准

---

## 开发依赖（仅开发环境）

| 包名 | 版本 | 用途 | 审计日期 | 备注 |
|------|------|------|----------|------|
| `pytest` | >=7.0.0 | 单元测试 | 2025-10-28 | 行业标准测试框架 |
| `pytest-cov` | >=4.0.0 | 测试覆盖率 | 2025-10-28 | pytest 官方插件 |
| `black` | >=23.0.0 | 代码格式化 | 2025-10-28 | PEP 8 格式化工具 |
| `mypy` | >=1.0.0 | 静态类型检查 | 2025-10-28 | 官方类型检查器 |
| `radon` | >=5.0.0 | 代码复杂度分析 | 2025-10-28 | 质量监控工具 |
| `pipdeptree` | >=2.0.0 | 依赖树可视化 | 2025-10-28 | 依赖审计辅助 |
| `pip-audit` | >=2.0.0 | 安全漏洞扫描 | 2025-10-28 | PyPA 官方工具 |

**备注**: 开发依赖不受零依赖原则约束，但仍优先选择维护良好的官方/行业标准工具。

---

## 依赖审计清单（模板）

添加新依赖前，必须填写以下清单：

### 包名: [PACKAGE_NAME]
**提议者**: [姓名]  
**提议日期**: [YYYY-MM-DD]  
**目标 extras 组**: [core/async/dev/etc]

#### 1. 必要性评估
- [ ] 无法使用标准库实现（说明原因）: 
- [ ] 功能对用户价值清晰（说明）:
- [ ] 考虑过替代方案（列出至少 2 个替代品及对比）:

#### 2. 维护活跃度
- GitHub 仓库: [URL]
- 最近提交日期: [日期]
- 开放 Issue 数: [数字]
- Issue 响应时间: [说明]
- **通过标准**: ✅ / ❌ (最近 6 个月有更新)

#### 3. 社区规模
- GitHub Stars: [数字]
- PyPI 月下载量: [数字]
- **通过标准**: ✅ / ❌ (stars > 100 或下载 > 10k/月)

#### 4. 许可证
- 许可证类型: [MIT/Apache/BSD/etc]
- 是否与项目兼容: ✅ / ❌
- **通过标准**: ✅ / ❌ (MIT/Apache/BSD 优先)

#### 5. 传递依赖
- 依赖数量: [数字]
- 依赖列表（运行 `pipdeptree -p PACKAGE_NAME`）:
```
[粘贴输出]
```
- **通过标准**: ✅ / ❌ (< 5 个)

#### 6. 安全性
- `pip-audit` 扫描结果: ✅ 无漏洞 / ❌ [列出漏洞]
- 历史安全问题: [说明或标记"无已知问题"]

#### 7. 最终决策
- [ ] **批准** - 满足所有标准，记录在 `requirements.txt` 和 `setup.py`
- [ ] **延期** - 需进一步评估 (理由: [说明])
- [ ] **拒绝** - 不符合宪章原则 (理由: [说明])

**批准人**: [姓名]  
**批准日期**: [YYYY-MM-DD]

---

## 定期审查

根据宪章**治理机制**，每季度审查依赖：

### 2025 Q4 审查 (2025-10-28)
- 核心依赖数: 0 ✅
- 未使用依赖: 无 ✅
- 安全漏洞: 无 ✅
- 过时依赖: 无 ✅

**审查人**: [姓名]  
**下次审查**: 2026-01-28

---

## 依赖移除记录

| 包名 | 移除日期 | 移除原因 | 影响范围 |
|------|----------|----------|----------|
| - | - | - | - |

**备注**: 记录移除历史以避免重复引入不适合的依赖。

---

## 参考工具

- `pipdeptree`: 查看依赖树
  ```bash
  pipdeptree
  ```

- `pip-audit`: 安全扫描
  ```bash
  pip-audit
  ```

- `pip list --outdated`: 检查过时依赖
  ```bash
  pip list --outdated
  ```

- `deptry`: 检测未使用依赖（需安装）
  ```bash
  pip install deptry
  deptry .
  ```

---

**最后更新**: 2025-10-28  
**负责人**: 项目维护团队

